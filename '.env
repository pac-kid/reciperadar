# 🧠 Node environment
NODE_ENV=production

# 🚪 Port Render will assign dynamically
PORT=10000

# 🌐 Allow requests only from your Vercel frontend (important for CORS)
CORS_ORIGIN=https://offer.vercel.app

# 🪪 (Optional) Session secret or API keys if you use authentication
SESSION_SECRET=your-secret-key-here

# 💾 (Optional) Database or API keys (add as needed)
# DATABASE_URL=postgres://...
# OPENAI_API_KEY=...

# ✅ Safety: avoid errors if fetch tries HTTP instead of HTTPS
FORCE_HTTPS=true
